尽管本文档以 **Visual Basic 或 VBA** 调用自动化服务器为视角编写，但**任何支持调用 COM 接口的语言**均可使用。

**不支持**直接从 **托管代码（如 C#）** 调用自动化接口；若必须这样做，需自行编写 **托管/非托管互操作包装器（wrapper）**。

[另请参阅：
从 C++ 调用自动化接口](从其他语言调用自动化接口/另请参阅+从+C+++调用自动化接口.md)

